<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Removing childNode</h1>
    <h2>I am in hyd</h2>
    <ul>
    <li>Guna</li>
    <li>Purushotham</li>
    </ul>
<script>
    const ul=document.querySelector('ul')
    const lists=document.querySelectorAll('li')
    console.log(ul)
    console.log(lists)
    for(const list of lists){
    console.log(`Removing :${list.textContent}`)
    ul.removeChild(list)
    console.log(lists)
    console.log(list)  
    }

    <h1>Removing childNode</h1>
    <h2>I am in hyd</h2>
    <ul>
    <li>Guna</li>
    <li>Purushotham</li>
    </ul>
<script>
    const ul=document.querySelector('ul')
    const lists=document.querySelectorAll('li')
    console.log(ul)
    console.log(lists)
    for(const list of lists){
    console.log(`Removing :${list.textContent}`)
    ul.removeChild(list)
    console.log(lists)
    console.log(list)  
    }


</script>

</script>
</body>
</html>

How to remove child elemenrts from a parent node.
removeChild()- to remove child elements.
Ex:
<h1>Removing childNode</h1>
<h2>I am in hyd</h2>
<ul>
<li>Guna</li>
<li>Purushotham</li>
</ul>
<script>
Const ul=document.querySelector(“ul”)
Const lists=document.querySelectorAll(“li”)
Console.log(ul)
Console.log(lists)
For(const list of lists){
Console.log(`Removing :${list.textContent)`)
Ul.removeChild(list)
Console.log(lists)
Console.log(list)
</script>
To append a new element to the parent node.
    <div id="a">
        <h1>Guna</h1>
        <h1>Ashish</h1>
        <h1>Chiru</h1>
    </div>
    <div id="b">
        <h2>Hema</h2>
        <h2>Purushotham</h2>
        <h2>Harshitha</h2>
    </div>
    <script>
        let n=document.createElement('h2')
        n.textContent='Guru'

        let p=document.getElementById('b')
        p.appendChild(n)

        console.log(p.innerText)
    </script>
